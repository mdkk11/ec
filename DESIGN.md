# MockShop — EC Design System

## 1. このドキュメントの目的

`MockShop` は、ファッションと生活雑貨を扱う架空のセレクトECです。
このドキュメントは、トップページを起点に一覧・詳細・カートへ展開できる、共通のデザイン判断を定義します。

参考: [FARFETCH 日本版](https://www.farfetch.com/jp/)

参考サイトからは、次の考え方を抽出しています。

- 商品写真を主役にし、UIの色と装飾を抑える
- ヘッダーにブランド、カテゴリ、検索、ユーザー操作を集約する
- 大きな余白と明確な文字階層で、ラグジュアリー感と可読性を両立する
- カテゴリ導線と商品発見を、編集的なビジュアルでつなぐ

ロゴ、固有コピー、レイアウトの数値、写真、アイコンは独自のものを使用します。参考サイトの複製ではなく、上記の原則を `MockShop` のブランド体験へ翻訳します。

## 2. ブランドコンセプト

### ポジショニング

日常に長く残る服と道具を、静かな編集で届けるオンラインストア。

### キーワード

- Quiet
- Editorial
- Tactile
- Considered
- Contemporary

### ブランドボイス

- 短く、落ち着いて、具体的に書く
- 過度な煽りや感嘆符を使わない
- 「買う」だけでなく、素材・背景・使い方を伝える
- 操作、状態、エラーは日本語を主体にする
- Vite試作から引き継ぐブランド名、シーズン名、編集見出しは固有コピーとして英語表記を維持する

### サンプルコピー

- メイン: 「季節の輪郭を、軽やかに。」
- 補足: 「新しい季節のための、静かな色と心地よい素材。」
- CTA: 「新着を見る」「コレクションを見る」

## 3. デザイン原則

1. **写真を最初に読む**
   商品写真とスタイリング写真に十分な面積を与え、UIは邪魔をしない。

2. **余白を情報として使う**
   セクション間を線で分けすぎず、余白の増減で階層を伝える。

3. **選択肢を一度に見せすぎない**
   ファーストビューの主CTAは2つまで。商品カードの常時表示情報はブランド、商品名、価格まで。

4. **モノクロを基調に、季節色を一点だけ使う**
   基本UIは白・墨色・グレー。キャンペーンや選択状態にアクセントカラーを使う。

5. **動きは理解を助ける範囲に限定する**
   hover、メニュー開閉、カート追加など、状態変化の説明にだけモーションを使う。

## 4. ビジュアルシステム

### カラーパレット

| Token | 値 | 用途 |
|---|---:|---|
| `canvas` | `#F6F5F2` | ページ背景 |
| `surface` | `#FFFFFF` | カード、ヘッダー |
| `ink` | `#171714` | 見出し、本文、主要アイコン |
| `muted` | `#706F69` | 補助文、メタ情報 |
| `line` | `#DDDCD6` | 境界線、入力欄 |
| `accent` | `#B5472F` | セール、選択状態、通知 |
| `sage` | `#BAC3B5` | 季節の背景色 |

コントラストの低いグレー文字は補助情報だけに使用し、本文は `ink` を基本にします。

### タイポグラフィ

- UI・本文: `Inter`, `Noto Sans JP`, sans-serif
- 編集的な大見出し: `Cormorant Garamond`, `Noto Serif JP`, serif
- ロゴ: 太めの sans-serif、字間を広く取る
- 数字: tabular nums を価格・件数で使用

| Style | Desktop | Mobile | Weight / Line height |
|---|---:|---:|---|
| Display | 72px | 44px | 500 / 0.98 |
| H1 | 48px | 36px | 500 / 1.1 |
| H2 | 32px | 28px | 500 / 1.2 |
| H3 | 20px | 18px | 600 / 1.35 |
| Body | 15px | 14px | 400 / 1.7 |
| Label | 12px | 11px | 600 / 1.4、字間 0.12em |

### 余白

4pxを基準に、主要値は `8 / 12 / 16 / 24 / 32 / 48 / 64 / 96 / 128`。

- ページ左右: desktop 32px、tablet 24px、mobile 16px
- セクション間: desktop 112〜144px、mobile 72〜96px
- カード間: desktop 20〜24px、mobile 12px
- コンテンツ最大幅: 1600px

### 角丸・線・影

- 角丸はボタン・入力欄で `9999px`、カードは原則 `0`
- 線は `1px solid line`
- 通常カードに影は付けない
- モーダルとドロワーにのみ、薄く広い影を使う

### アイコン

- 24pxのアウトラインアイコン
- 線幅は 1.5px
- ラベルがなくても一般的に理解できる操作に限定
- hover / focus時は背景の円または下線で反応を示す

### 写真

- 自然光、ニュートラルな色温度、素材感がわかる写真
- 人物写真は動きの途中を切り取ったような構図
- 商品写真は淡い単色背景、余白を広く取る
- 同一グリッド内で彩度を揃える
- 画像比率: ヒーロー `16:10`、編集カード `4:5`、商品 `3:4`

## 5. レイアウト

### グリッド

- Desktop: 12カラム、最大1600px
- Tablet: 8カラム
- Mobile: 4カラム

### ヘッダー

Desktopは3層構造です。

1. アナウンスバー: 送料無料やキャンペーンを1行で表示
2. メインバー: 左にカテゴリ、中央にロゴ、右に検索・アカウント・お気に入り・カート
3. 必要に応じてサブナビ: 新着、ウェア、バッグなど

Mobileはロゴを中央に置き、左にメニュー、右に検索とカートを配置します。カテゴリはドロワー内に移動します。

上記は各機能の実装後に目指す構成です。Phase 1では利用可能なページ内ナビゲーションだけを表示し、検索・アカウント・お気に入り・カートの操作を見た目だけで先行実装しません。

### トップページ

1. ヘッダー
2. ヒーロー: 左にコピー、右にスタイリング写真
3. 編集カテゴリ: 3枚のビジュアルカード
4. 新着商品: Desktop 4列、Tablet 3列、Mobile 2列
5. ブランドステートメント
6. メルマガ登録
7. フッター

### 商品一覧

- 上部にパンくず、タイトル、説明、件数
- Mobileは2列、Tabletは3列、Desktopは4列の商品グリッド
- 商品は固定された新着順で表示し、検索、絞り込み、利用者が選ぶ並び替えは設けない

### 商品詳細

- Desktopは左2/3に画像、右1/3に購入情報をsticky表示
- Mobileは単一の商品画像の後に商品情報を表示
- 単一SKUのため、色・サイズ選択は設けない
- カート追加操作はカート機能を実装するPhaseで接続する

## 6. コンポーネント仕様

### Button

- Primary: `ink` 背景 + 白文字
- Secondary: 透明背景 + `ink` の1px線
- Text: 下線付きテキスト
- 高さ: 48px、mobileもタップ領域44px以上
- disabledは不透明度だけに頼らず、カーソルと文言で状態を伝える

### Product Card

- 動的な商品カードは画像領域、商品名、価格、在庫状態で構成
- 画像hoverでは1.02倍まで拡大
- 商品名は長い場合も切り捨てず、カード内で折り返す
- 画像には具体的な代替テキストを付ける

Phase 1のトップページにある静的な商品プレビューは編集表現として画像、ブランド名、商品名、色、価格を維持します。動的なProduct Cardへブランド・色を要求せず、お気に入りは追加しません。カート操作はカート機能を実装するPhaseで追加します。

### Editorial Card

- 写真の下に eyebrow、見出し、短い説明、テキストリンク
- 写真上の文字重ねはコントラストが確保できる場合のみ

### Search

- Desktopはヘッダー内の下線型入力、Mobileは全画面オーバーレイ
- 入力中は候補カテゴリと最近見た商品を表示
- `Escape` で閉じられる

### Toast

- カート追加やお気に入り保存の結果を短く通知
- 画面右下、Mobileは下中央
- 4秒後に消えるが、スクリーンリーダーへは `aria-live="polite"` で伝える

## 7. インタラクションとモーション

- 標準transition: `180ms ease-out`
- ドロワー: `240ms cubic-bezier(0.22, 1, 0.36, 1)`
- 画像hover: `400ms ease`
- `prefers-reduced-motion: reduce` では移動と拡大を停止
- focus ringは `2px`、`ink`、offset `3px`
- hoverだけに意味を持たせない

## 8. レスポンシブ

Tailwindの標準breakpointを基本にします。

| Range | 方針 |
|---|---|
| `< 640px` | 2列商品、1列編集カード、モバイルヘッダー |
| `640–1023px` | 2〜3列商品、ヒーローは比率を圧縮 |
| `≥ 1024px` | 4列商品、左右分割ヒーロー、デスクトップヘッダー |
| `≥ 1536px` | 最大幅を固定し、画像サイズではなく余白を広げる |

## 9. アクセシビリティ

- 文字と背景のコントラストはWCAG AAを目標にする
- キーボード操作だけでナビ、商品選択、実装済みPhaseのカート追加を完了できる
- アイコンボタンに `aria-label` を付ける
- 見出しレベルを飛ばさない
- 色だけでセール・選択・エラーを区別しない
- 動的な商品画像のaltは商品名を基本にし、静的な編集画像は内容を具体的に表す
- モーダル表示中は背景へフォーカスを移動させない

## 10. Tailwind実装ルール

- 色、フォント、影、transitionはCSS変数またはテーマトークンへ寄せる
- 同じclass列が3回以上登場したらコンポーネント化を検討する
- 任意値は画像比率やレイアウト上必要な場合に限定する
- `cn` ユーティリティは条件付きclassが必要になってから導入する
- UI状態はclassとARIA属性の両方へ反映する

想定トークン:

```css
:root {
  --color-canvas: #f6f5f2;
  --color-surface: #ffffff;
  --color-ink: #171714;
  --color-muted: #706f69;
  --color-line: #dddcd6;
  --color-accent: #b5472f;
  --color-sage: #bac3b5;
}
```

## 11. サンプル画面のスコープ

今回のサンプルではトップページを実装します。Vite試作の情報構成、コピー、ビジュアルはNext.jsへ引き継ぎますが、コード互換性は維持しません。

- アナウンスバーとレスポンシブヘッダー
- ヒーロー
- 3つの編集カテゴリ
- 4つの新着商品カード
- ブランドステートメント、メルマガ、フッター

Vite試作に含まれていた検索、お気に入り、仮カート、メルマガ登録のデモ操作は移植しません。商品一覧、商品詳細、認証、カート、注文、実API接続は開発計画の各Phaseで実装します。

## 12. 品質チェックリスト

- 375px / 768px / 1440pxでレイアウトが破綻しない
- すべての操作要素にhover / focus / active状態がある
- キーボードだけで主要操作ができる
- 画像に幅・高さまたはaspect-ratioがあり、レイアウトシフトが少ない
- 主要画像にaltがある
- 主要ページのH1は1つ
- LighthouseのPerformance / Accessibility / Best Practices / SEOで90以上を目標にする
- `pnpm build` が成功する
